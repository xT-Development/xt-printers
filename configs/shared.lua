return {
    debugPrints = false,         -- enable debug prints

    maxPaper = 100,             -- max paper allowed in printers

    allowedJobs = {             -- jobs allowed to create/manage printers. Equal to the minimum job level required to create/manage printers
        police = 0,
        ambulance = 0,
        mechanic = 0
    }
}